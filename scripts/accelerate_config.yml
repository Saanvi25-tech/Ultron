compute_environment: 'LOCAL_MACHINE'
deepspeed_config: {}
distributed_type: 'NO'
downcast_bfloat16: false
fp16: true
machine_rank: 0
main_process_ip: '127.0.0.1'
main_process_port: 29500
mixed_precision: 'fp16'
num_machines: 1
num_processes: 1
use_cpu: false
use_deepspeed: false
use_mps_device: false

# If using bitsandbytes + offloading on a single A100, you may instead use device_map='auto' and offload config in your training code.
